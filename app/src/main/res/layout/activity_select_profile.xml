<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/pilih_profil"
        android:layout_margin="16dp"
        android:textSize="18sp"
        android:fontFamily="@font/inter_tight_semi_bold"
        android:layout_marginBottom="16dp"/>

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/tv_no_profile"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Belum ada profil.\nSilakan klik Tambahkan Akun untuk membuat profil baru sebelum melanjutkan."
        android:fontFamily="@font/inter_tight_regular"
        android:textSize="12sp"
        android:visibility="gone"
        android:layout_margin="16dp"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvProfiles"
        android:layout_margin="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnAddProfile"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:visibility="gone"
        android:backgroundTint="#ECEFF3"
        android:text="@string/tambahkan_akun"
        android:textColor="#272835"
        app:icon="@drawable/ic_add"
        app:iconTint="#272835"
        app:iconGravity="textStart"
        android:layout_marginTop="16dp"/>
</LinearLayout>
